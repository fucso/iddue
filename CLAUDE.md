# CLAUDE.md

## リポジトリ概要

このリポジトリは GitHub Issues を起点とした開発ワークフロー用のスキルコレクションです。`iddue:*` スキル群（起票・実装・レビュー・フィードバック対応）と `github-*` ユーティリティ（PR 情報取得・差分取得・レビュー投稿・スレッド操作）で構成されており、`.claude/` ディレクトリをコピーすることで任意のプロジェクトリポジトリで共通利用できます。

## 開発ルール

### 環境非依存

スキルは特定の言語・フレームワーク・ランタイムに依存しない汎用実装にしてください。このリポジトリは多様なプロジェクトリポジトリにコピーして利用されるため、特定の環境を前提とした実装は利用範囲を狭めます。

### gh コマンドの利用

GitHub API との通信は `gh` CLI のみを使用してください。`curl` や SDK の直接呼び出しは禁止です。リポジトリ側の MCP 設定（GitHub MCP Server など）に依存しないようにするためです。`gh` は認証・エンドポイント解決・エラーハンドリングを一貫して提供するため、スクリプトの移植性も確保されます。

### スキル間の依存整理

スキルは呼び出し関係によって暗黙的な依存ツリーを構成します。この依存関係を逆流させるような参照はしないでください。具体的には、SKILL.md や関連ドキュメントに「〜スキルから呼ばれる」「〜が依存している」といった被依存方向の記述を含めることは NG です。依存ツリーの上位（呼び出し元）は下位（呼び出し先）の変更に追従する責任を持ちますが、下位は上位を知らない設計にする必要があります。

### スクリプト化

AI の推論を必要としない定型処理（API 呼び出し・ファイル操作・コマンド実行）はシェルスクリプトに分離して `scripts/` に配置してください。スキルの SKILL.md には「何をするか」を記述し、「どうやるか」の実装詳細はスクリプトに委ねることで、スキルの可読性と保守性を高めます。

### ドキュメント内の採番ルール

SKILL.md などのドキュメントに Phase・Step などの番号を追加・挿入する場合は、`2.2.5` や `4.0` のような中間値を差し込まず、既存の番号を後ろにずらして自然な連番を維持してください。

例: Phase 2.2 と 2.3 の間に新しいステップを追加する場合 → 新ステップを 2.3 とし、既存の 2.3 以降を 2.4、2.5… に繰り上げる。

### 変更時のドキュメント更新

スキルの仕様やインターフェースを変更した場合は、同一 PR 内で以下のドキュメントを更新してください。

- 変更したスキル自身の SKILL.md および `references/` 配下のドキュメント（内部の生合成を保つ）
- そのスキルを呼び出している被依存スキルの SKILL.md（呼び出し方法の変更に追従させる）
- README.md・CLAUDE.md（利用者・開発者向けの情報として影響がある場合）
