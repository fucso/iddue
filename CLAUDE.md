# CLAUDE.md

## リポジトリ概要

- **idd:* スキル群**: IDD ワークフローのコア実装（起票・実装・レビュー・フィードバック対応）
- **github-* ユーティリティ**: GitHub API 操作の共通スクリプト群（PR 情報取得・差分取得・レビュー投稿・スレッド操作）
- **複数リポジトリ共通利用**: `.claude/` ディレクトリをコピーして任意のプロジェクトリポジトリで利用できる設計

## 開発ルール

- **環境非依存**: スキルは特定の言語・フレームワーク・ランタイムに依存しない汎用実装とする
- **gh コマンド限定**: GitHub API との通信は `gh` CLI のみを使用する（`curl`・SDK 直接呼び出し禁止）
- **スキル依存方向**: 呼び出し方向は `ユーザー → コマンドスキル → 内部スキル` の一方向とする（内部スキルがユーザー起動スキルを呼び出すことは禁止）
- **SKILL.md 制約**: 各スキルのエントリーポイントは `SKILL.md` のみ。スキル間の連携は SKILL.md に記載されたインターフェースを通じて行う
- **スクリプト化**: AI の推論不要な定型処理（API 呼び出し・ファイル操作・コマンド実行）はシェルスクリプトに分離して `scripts/` に配置する
- **変更時ドキュメント更新**: スキルの仕様・インターフェースを変更した場合は同一 PR 内で対応する SKILL.md・references/ を更新する
